---
import { getCollection } from "astro:content";
const { showAll = true, limit = 6 } = Astro.props;

const allStaff = await getCollection("staff", ({ data }) =>
  import.meta.env.PROD ? !data.draft : true
);
const staffMembers = allStaff
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, showAll ? undefined : limit);
---

<!-- ================= STAFF GRID ================= -->
<section id="staff" class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl font-bold text-center mb-4">Nuestro Equipo de Liderazgo</h2>
    <p class="text-center text-gray-600 mb-12">
      Conoce al equipo de liderazgo y académicos que impulsan la ciencia de datos en SOCIDAIN.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 auto-rows-fr">
      {staffMembers.map(staff => (
        <div class="bg-white rounded-lg shadow overflow-hidden flex flex-col h-full hover:shadow-lg transition-shadow">
          <!-- Foto (1:1) -->
          <div class="relative w-full pb-[100%] bg-gray-100">
            {staff.data.image && !staff.data.image.includes("default-avatar") ? (
              <img
                src={staff.data.image}
                alt={staff.data.name}
                class="absolute inset-0 w-full h-full object-cover object-center"
              />
            ) : (
              <div class="absolute inset-0 w-full h-full flex items-center justify-center bg-gradient-to-br from-gray-100 to-gray-200">
                <span class="text-5xl font-bold text-gray-400 select-none">
                  {staff.data.name.split(" ").map((n: string) => n[0]).join("").slice(0, 2).toUpperCase()}
                </span>
              </div>
            )}
          </div>
          <div class="p-5 flex flex-col flex-1 text-center bg-white">
            <div class="mb-4">
              <h3 class="text-xl font-bold mb-1 text-gray-900">{staff.data.name}</h3>
              <p class="text-primary-800 font-bold text-xs uppercase tracking-widest">{staff.data.title}</p>
            </div>
            <div class="space-y-1 text-sm border-t border-gray-100 pt-4 mt-auto">
              {staff.data.email && (
                <a
                  href={`mailto:${staff.data.email}`}
                  class="text-gray-500 hover:text-primary-600 block truncate transition-colors"
                  title={staff.data.email}
                >
                  {staff.data.email}
                </a>
              )}
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>